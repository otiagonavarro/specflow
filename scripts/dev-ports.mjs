/**
 * Default dev ports — high range (58k) to reduce collisions with 3000, 5173, 8080, etc.
 * Keep vite.config.ts, server/index.ts, package.json "dev", and .env.example in sync.
 */
export const DEFAULT_UI_DEV_PORT = 58470;
export const DEFAULT_API_PORT = 58471;
